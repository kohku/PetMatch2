<% content_for :title, @page.title %>
<div class="container">
    <div class="row-fluid">
    	<h3><%= @page.title %></h3>
    	<div class="custom_panel margin-bottom-10">
    		<%= @page.content %>
		</div>
	</div>
</div>