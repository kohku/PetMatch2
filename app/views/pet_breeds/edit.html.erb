<div class="container">
    <div class="row-fluid">
    	<h3>Editing <%= [@pet_breed.pet_type.name, @pet_breed.name].join(' ') %></h3>

<%= render 'form' %>

<%= link_to 'Back', pet_type_path(@pet_breed.pet_type), :class => 'btn btn-default btn-small' %>
	</div>
</div>